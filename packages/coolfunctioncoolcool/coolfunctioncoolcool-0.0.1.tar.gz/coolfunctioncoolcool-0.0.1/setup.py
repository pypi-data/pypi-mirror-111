from setuptools import setup, find_packages
import codecs
import os

setup(
    name='coolfunctioncoolcool',
    version = '0.0.1',
    author = 'RaphaelKalfon',
    author_email = 'raphaelkalfon9@gmail.com',
    description = 'Say hello!',
    packages = find_packages(),
    install_requires = [],
    keywords = ['python'],
)

# Run the pip install -e . everytime you change the setup
