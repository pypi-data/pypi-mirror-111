from enum import Enum


class AxisType(Enum):
    """
    A enumeration for representation the axis (X or Y).
    """
    X = 0
    Y = 1


class SectionType(Enum):
    """
    A enumeration for representing the distinct section inside a image.
    """
    S0 = 0
    S1 = 0
    S2 = 0
    S3 = 0
