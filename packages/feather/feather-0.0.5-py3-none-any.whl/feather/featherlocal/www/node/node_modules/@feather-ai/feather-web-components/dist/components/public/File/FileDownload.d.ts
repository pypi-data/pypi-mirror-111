import React from "react";
import { BaseProps } from "../../../utils/typeUtils";
declare type Schema = {
    data: string;
    name: string;
};
export declare type FileDownloadProps = BaseProps & {
    files: Schema[];
    outputFilenames?: string[];
    title?: string;
    description?: string;
};
declare const FileDownload: React.FC<FileDownloadProps>;
export default FileDownload;
