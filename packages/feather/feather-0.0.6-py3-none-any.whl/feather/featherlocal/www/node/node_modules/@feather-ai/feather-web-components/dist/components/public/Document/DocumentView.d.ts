import React from "react";
import { BaseProps } from "../../../utils/typeUtils";
declare type Schema = {
    data: string;
    name: string | null;
    text: string;
};
export declare type DocumentViewProps = BaseProps & {
    documents: Schema[];
};
declare const DocumentView: React.FC<DocumentViewProps>;
export default DocumentView;
