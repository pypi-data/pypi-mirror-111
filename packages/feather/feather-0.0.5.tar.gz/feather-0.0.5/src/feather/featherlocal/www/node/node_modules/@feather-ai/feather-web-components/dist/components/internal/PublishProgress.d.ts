import React from "react";
declare type PublishProgressProps = {
    currFile: number;
    totalFiles: number | string;
};
declare const PublishProgress: React.FC<PublishProgressProps>;
export default PublishProgress;
