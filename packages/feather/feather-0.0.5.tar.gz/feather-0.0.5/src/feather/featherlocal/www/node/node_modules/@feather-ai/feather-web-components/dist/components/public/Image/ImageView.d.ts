import React from "react";
import { BaseProps } from "../../../utils/typeUtils";
declare type Schema = {
    data: string;
    text: string;
    name: string;
};
export declare type ImageViewProps = BaseProps & {
    images: Schema[];
};
declare const ImageView: React.FC<ImageViewProps>;
export default ImageView;
