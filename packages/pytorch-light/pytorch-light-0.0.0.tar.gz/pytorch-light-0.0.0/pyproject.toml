[tool.poetry]
name = "pytorch-light"
version = "0.0.0"
description = "Light."
authors = ["Yevgnen Koh <wherejoystarts@gmail.com>"]
packages = [ { include = "light" } ]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
